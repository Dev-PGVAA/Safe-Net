.DS_Store
._*
.turbo
.claude/

# dependencies
node_modules/

# env
.env
.env.local
.env*.local

# builds
.next/
dist/

# certificates / keys — never commit, generate locally (mkcert)
*.pem
*.key

# local-only: retired code and database dumps. Dumps contain real user rows
# and password hashes, so this must never be tracked.
_archive/


# python virtualenv (ml-service)
.venv/
__pycache__/
*.pyc

# extension build artifacts (wxt)
.output/
.wxt/
